\documentclass[a4paper,12pt]{article}

\usepackage{times}                       % 使用 Times New Roman 字体
\usepackage{CJK,CJKnumb,CJKulem}         % 中文支持宏包
\usepackage{color}                       % 支持彩色
\usepackage{amsthm} %数学定理
\usepackage{amsmath} %数学符号
\usepackage{amssymb} %数学符号
\usepackage{indentfirst} %缩进
\usepackage{graphicx} %图形支持
\usepackage{listings} %源代码插入
\usepackage{bm} %加粗

\usepackage{float} %处理图片位置
%——————————–其他宏包——————————
%\usepackage{amsmath,amsthm,amsfonts,amssymb,bm} % 数学宏包
%\usepackage{graphicx,psfrag}                    % 图形宏包
%\usepackage{makeidx}                            % 建立索引宏包
%\usepackage{listings}                           % 源代码宏包
%———————————正文———————————–
% 换行 \\
% 换页 \clearpage

\begin{document} % 开始正文
\begin{CJK*}{GBK}{song}                           % 开始中文环境
\newtheorem{theorem}{定理}[section]
\newtheorem{lemma}{引理}[section]
\newtheorem{definition}{定义}[section]
\newtheorem*{myproof}{证明}
\newtheorem*{myexercise}{练习}
\setlength{\parindent}{2em}
\author{孙锴}                                 % 作者
\title{Homework 13}                                % 题目
\maketitle                                       % 生成标题
\begin{myexercise}[6.1]
取$\mathbf{w}=\{1,1,\ldots,1\}$，$b=\frac{1}{2}$，
则易见$(\mathbf{w}, b)$是一个linear separator。由margin定义，margin=$min_{i}\frac{\mathbf{\hat{w}}\cdot \mathbf{\hat{a_{i}}}l_{i}}{|\mathbf{\hat{w}}|}=\frac{1}{\sqrt{4d+1}}=\Omega(\frac{1}{d})$。
\end{myexercise}
\begin{myexercise}[6.4]
本题即证异或函数是线性不可分的。\\
（反证法）假设异或函数是线性可分的，那么设$(\mathbf{w}, b)$是一个linear separator。考虑$\mathbf{\hat{w}}\mathbf{\hat{a_{i}}}$ (*)，其中$\mathbf{\hat{w}}=\{x_{1},x_{2},\ldots,x_{d},-b\}$，$\mathbf{\hat{a_{i}}}=\{a_{1},a_{2},\ldots,a_{d},1\}$。\\
对于$a_{1}=a_{2}=\ldots =a_{d}=0$时，有(*)式$<0$，从而$b>0$；(*****)\\
对于$a_{1}=1, a_{2}=a_{3}=\ldots = a_{d}=0$，有(*)式$>0$，从而$x_{1}>b$；(**)\\
对于$a_{2}=1, a_{1}=a_{3}=a_{4}\ldots = a_{d}=0$，有(*)式$>0$，从而$x_{2}>b$；(***)\\
对于$a_{1}=a_{2}=1, a_{3}=a_{4}\ldots = a_{d}=0$，有(*)式$<0$，从而$x_{1}+x_{2}<b$；(****)\\
由(**)(***)(****)得$b>x_{1}+x_{2}>2b$，从而$b<0$，这与(*****)矛盾，所以假设不成立。从而异或函数是线性不可分的。\\
%实际上，本问题早期人工神经网络中的感知器的局限性的一个重要支撑。
\end{myexercise}
\begin{myexercise}[6.13]
令$\mathbf{\varphi} (\mathbf{x})=\{x_{1}^{2}, x_{2}^{2}\}$，则在$\mathbf{\varphi}$中取$\mathbf{w}=\{-1,-1\}$，$b=-1$，那么易见$(\mathbf{w}, b)$是一个linear separator。
\end{myexercise}
\begin{myexercise}[6.19]
我认为题目本意是要求描述用boosting算法由weak learner构造strong learner的方法，并解释如何将构造出的strong learner应用于新的数据。\\
设初始时weak learner为$h_{0}(x)$，第1次迭代调整权重后的weak learner为$h_{1}(x)$，第2次迭代调整权重后的weak learner为$h_{2}(x)$，$\ldots$，第T次迭代调整权重后的weak learner为$h_{T}(x)$。则在$T=\Omega(logn)$前提下，可以构造strong learner$H(x)=sgn(\sum_{i=0}^{T}h_{i}(x))$。\\
由于构造出的H(x)对训练数据的分类能力非常好，因此对于新的数据，可以认为H(x)可以以较大概率给出正确的分类，即若a是一个新的数据，那么$H(a)$即为给出的分类。\\
\end{myexercise}
\begin{myexercise}[6.21]
设$S={a_{1},a_{2},\ldots,a_{n}}$为n个点的集合，记$C_{1}$的圆心为$o_{1}$，取$r_{1}=max\{|a_{1}-o_{1}|, |a_{2}-o_{1}|, \ldots, |a_{n}-o_{1}|\}$，则以$o_{1}$为圆心，$r_{1}$为半径的圆包含与$C_{1}$相同的点集，同时有至少一个点在其圆周上，记这个圆为$C_{1}^{'}$。
若$C_{1}^{'}$上有两个或者两个以上的点，则只须令$C_{2}$取$C_{1}^{'}$，否则，记在$C_{1}^{'}$圆周上的点为$o_{2}$，令$d=max\{k>0|\exists a_{i}, |a_{i}-o_{2}|^{2}+|a_{i}-[k(o_{1}-o_{2})+o_{2}]|^{2}=|k(o_{1}-o_{2})|^{2}\}$，则令$C_{2}$为以$o=\frac{d(o_{1}-o_{2})}{2}+o_{2}$为圆心，$r=\frac{d}{2}$的圆，则显然$C_{2}$的圆周上至少有两个点。
\end{myexercise}
\end{CJK*}     % 结束中文环境
\end{document} % 结束正文 